server:
  port: 8084

spring:
  application:
    name: mall-order
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://dingchunru.cn:3306/mall_order?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 7259.Dcr.
  redis:
    host: dingchunru.cn
    port: 6379
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

rocketmq:
  name-server: dingchunru.cn:9876
  producer:
    group: mall-order-producer
    send-message-timeout: 3000
    retry-times-when-send-failed: 3
  consumer:
    group: mall-order-consumer
    pull-batch-size: 32
    max-reconsume-times: 16

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.mall.order.entity
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.mall.order: debug

jwt:
  secret: mall-secret-key-must-be-at-least-32-bytes-long!!
  access-token-expiration: 1800
  refresh-token-expiration: 604800
  issuer: mall-server